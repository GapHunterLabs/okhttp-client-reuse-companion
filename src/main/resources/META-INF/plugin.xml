<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>dev.gaphunter.okhttpclientreusecompanion</id>
    <name>OkHttp Client Reuse Companion</name>
    <vendor>Gap Hunter Labs</vendor>
    <description><![CDATA[
        <p>Gutter warning icon on an <code>OkHttpClient</code> built
        via <code>new OkHttpClient()</code> or
        <code>new OkHttpClient.Builder().build()</code> inside a
        regular method body -- OkHttp's own documentation states
        "OkHttp performs best when you create a single OkHttpClient
        instance and reuse it for all of your HTTP calls, because each
        client holds its own connection pool and thread pools...
        creating a client for each request wastes resources on idle
        pools". Building one inside a method means a brand new
        connection pool (and thread pools) gets created on every
        call.</p>
        <p><b>100% static text/PSI analysis</b> -- matches the class
        name by simple text, so it works whether the real OkHttp jar
        is on the classpath or not. Java and Kotlin.</p>
        <p>Stated honestly: v0.1 only flags the "build from scratch"
        shape -- <code>existingClient.newBuilder()....build()</code>,
        OkHttp's own documented pattern for customizing a shared client
        without duplicating its connection pool, is never flagged
        (correctly, since it isn't the anti-pattern this plugin
        targets). Never flags a build call inside a constructor or a
        field/property initializer.</p>
        <p>Free. No license nags, no telemetry, no network calls.</p>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.java</depends>
    <depends>org.jetbrains.kotlin</depends>

    <extensions defaultExtensionNs="org.jetbrains.kotlin">
        <supportsKotlinPluginMode supportsK1="true" supportsK2="true"/>
    </extensions>

    <extensions defaultExtensionNs="com.intellij">
        <codeInsight.lineMarkerProvider language="JAVA"
            implementationClass="dev.gaphunter.okhttpclientreusecompanion.gutter.ClientBuiltPerCallLineMarkerProvider"/>
        <codeInsight.lineMarkerProvider language="kotlin"
            implementationClass="dev.gaphunter.okhttpclientreusecompanion.gutter.ClientBuiltPerCallLineMarkerProvider"/>
        <notificationGroup id="OkHttp Client Reuse Companion" displayType="BALLOON"/>
    </extensions>
</idea-plugin>
